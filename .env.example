# =============================================================================
# Universal Robots + ROS2 Configuration
# =============================================================================
# Copy this file to .env and modify as needed
# =============================================================================

# -----------------------------------------------------------------------------
# ROS2 Configuration
# -----------------------------------------------------------------------------
# Available: humble, jazzy, rolling
ROS_DISTRO=humble

# ROS2 Domain ID (0-232, use different IDs for network isolation)
ROS_DOMAIN_ID=0

# DDS Implementation (rmw_cyclonedds_cpp recommended for Docker)
RMW_IMPLEMENTATION=rmw_cyclonedds_cpp

# Build driver from source instead of apt packages
BUILD_FROM_SOURCE=false

# -----------------------------------------------------------------------------
# Robot Configuration
# -----------------------------------------------------------------------------
# Robot type: ur3e, ur5e, ur10e, ur16e, ur20, ur30
UR_TYPE=ur5e

# Robot model for URSim: UR3, UR5, UR7, UR10, UR12, UR15, UR16, UR20, UR30
ROBOT_MODEL=UR5

# Robot IP address
# For simulation: use URSIM_IP (172.28.0.10)
# For real robot: use actual robot IP (e.g., 192.168.1.100)
ROBOT_IP=172.28.0.10

# -----------------------------------------------------------------------------
# URSim Configuration
# -----------------------------------------------------------------------------
URSIM_VERSION=latest
URCAP_VERSION=1.0.5
URSIM_IP=172.28.0.10

# -----------------------------------------------------------------------------
# Driver Configuration
# -----------------------------------------------------------------------------
DRIVER_IP=172.28.0.20
HEADLESS_MODE=true
LAUNCH_RVIZ=false

# -----------------------------------------------------------------------------
# Port Mapping (change if ports conflict with other services)
# -----------------------------------------------------------------------------
VNC_PORT=5900
WEB_VNC_PORT=6080
DASHBOARD_PORT=29999
PRIMARY_PORT=30001
SECONDARY_PORT=30002
RT_PORT=30003
RTDE_PORT=30004

# -----------------------------------------------------------------------------
# Web Interface Configuration
# -----------------------------------------------------------------------------
WEB_UI_PORT=8090

# -----------------------------------------------------------------------------
# Directories (relative to docker-compose.yml location)
# -----------------------------------------------------------------------------
PROGRAMS_DIR=./programs
CALIBRATION_DIR=./config/calibration
CONFIG_DIR=./config
WORKSPACE_DIR=./workspace

# -----------------------------------------------------------------------------
# Display (for RViz/GUI applications)
# -----------------------------------------------------------------------------
DISPLAY=:0
